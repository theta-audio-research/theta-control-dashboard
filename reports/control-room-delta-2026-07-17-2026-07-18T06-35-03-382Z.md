# THETA Control Room — Build Hook Delta
**Date:** 2026-07-17  
**Mode:** Automated build-script update.

## Changed projects

| Project | Change type | End status | Tomorrow's first action |
|---|---|---|---|
| CREOSOTE | Build hook update | Build hook reported CREOSOTE v0.4.35. | v0.4.35 — OS DRY-LEG ALIGNMENT (user: in OS CLEAN + clean mode, raising OS ALSO cuts the highs — v0.4.34's flat FIR wasn't enough). MEASURED ROOT CAUSE: wet/dry COMB, not filter rolloff. The CLEAN OS FIR delays the WET path 4/5/6 base samples at 2x/4x/8x; GRIT and AMBER's internal MIX blends (l = l*(1-mix) + wetL*mix) used an UNDELAYED dry, so partial MIX (user runs GRIT 35% / AMBER 72%) combed: measured -6dB@4k at 4x, -8dB@4k at 8x (48k, 35% mix) — first notch at fs/(2d) marches DOWN as factor rises = exactly 'raising OS cuts the highs'. An integer dry delay CANNOT fix it (min-phase FIR group delay is not constant — best case still ~6dB residual at 4x). FIX: run the dry mix leg through an IDENTICAL oversampler with an identity saturator (osDryL/R in GritModule + AmberModule) — both legs share one linear response, comb cancels exactly at every frequency; module-internal, plugin latency untouched (set once in prepareToPlay, unchanged). Gated GRIT>=7 / AMBER>=4 (osDryAlign, CLEAN+HQ only — VINTAGE would LPF the dry and its ~0-1 smp never combed; older stamps keep the uncompensated blend bit-identically). COMPRESS is 100% wet when active (no comb) and PAR routing died in v18.7.0 — these two module mixes were the only exposures. All 23 presets restamped 7/4/3. VERIFY IN ABLETON: 48k, MODE CLEAN, OS CHAR CLEAN, GRIT MIX 35% + AMBER MIX 72%, toggle OFF/2x/4x/8x — top end should now be IDENTICAL across all factors; at MIX 100% behavior is unchanged (comb only existed at partial mix). OPEN: painted-metal chassis; deferred patina; LIMITER rename. |

## Dashboard update decision

| Item | Result |
|---|---|
| Any project deltas? | Yes |
| Dashboard data changed? | Yes |
| Tomorrow Focus Mode priorities | Unchanged |
| Tomorrow's first Control Room action | Review build-hook update for CREOSOTE. |

## Notes

- Only changed projects were included.
