# THETA Control Room — Build Hook Delta
**Date:** 2026-07-18  
**Mode:** Automated build-script update.

## Changed projects

| Project | Change type | End status | Tomorrow's first action |
|---|---|---|---|
| CREOSOTE | Build hook update | Build hook reported CREOSOTE v0.4.39. | v0.4.39 — THE CALIBRATION RELEASE (user: 'every setting is always too much gain and too much distortion... instead of nice subtle saturation and color'). MEASURED ROOT CAUSES (4-agent extraction + exact Python THD map): (1) GRIT's asymmetry was SIGN-SWITCHED DRIVE (pos x1.30/neg x0.70) — a slope kink at zero, which is SCALE-INVARIANT: ~13% THD at EVERY level and EVERY knob position (12.9% @-18dBFS with DRIVE AT ZERO; real tape: 0.3-1%) plus +5.1dB linear gain on positive half-cycles. GRIT literally could not be subtle. (2) AMBER's drive law was linear over the tube's FULL maxGainDb (12AX7 28dB): default 0.3 = +8.4dB into the stage, 7.5% THD on -6dBFS peaks, all useful range in the bottom sixth of the knob. (3) COMPRESS CMP_INPUT mapped -6..+18dB so the 0.5 default = +6dB hidden input gain. (Also found: TubeProfiles.headroomDb pad column was designed but never wired; CHARACTER tiers and SUMMING iron measured correctly calibrated — iron was tuned at -18 by measurement, the template.) FIXES (user chose: hardware-true zero, -18dBFS=0VU ref, presets keep values, all at once): GRIT v9 — smooth bias-shift law u=g*(x+magBias)+tb, y=(tanh(u)-tanh(tb))/(g*(1-tanh(tb)^2)): evens from curvature (level-dependent, vanish at silence), EXACT unity small-signal gain, g=1.33*3.16^(drive*tapeScale) (10dB span), tb from machine asym ratio (H2 -50dB @0VU at zero), old compGain divisor dropped in v9 (was correcting the old law's normalization boost). MEASURED: DRIVE=0 -> 0.39% @0VU (33x cleaner), DRIVE=1 -> 2.8% @0VU / 18% @-6dB peaks = tape slam only when pushed. AMBER v6 — square taper gDb=drive^2*maxGainDb*0.828: 0 -> 0.49% @0VU, 0.5 -> 0.95%, 1.0 -> 10% driven-amp; whole knob useful. COMPRESS v4 — input map -12..+12 (0.5=unity). All gated; <v9/<v6/<v4 recall the old laws bit-identically. All 23 presets restamped 9/6/4 with knob values UNCHANGED (they get cleaner — user's choice). VERIFY IN ABLETON: 48k, peaks ~-6: GRIT at DRIVE 0-0.3 should now be SUBTLE COLOR (bump/EQ/flutter with barely-there sat), driving to 1.0 brings real tape compression; AMBER default warm not crunchy; old sessions unchanged. OPEN: freeze iron voice after audition; painted-metal chassis; deferred patina; LIMITER rename. |

## Dashboard update decision

| Item | Result |
|---|---|
| Any project deltas? | Yes |
| Dashboard data changed? | Yes |
| Tomorrow Focus Mode priorities | Unchanged |
| Tomorrow's first Control Room action | Review build-hook update for CREOSOTE. |

## Notes

- Only changed projects were included.
