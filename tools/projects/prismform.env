# THETA Control Room — per-project config for PRISM FORM
# Static identity only; the build supplies version/artifact/status at call time.
export PROJECT_NAME="PRISM FORM"
export PROJECT_ID="prismform"
export PROJECT_FORMATS_JSON='["VST3","AU"]'
# export PROJECT_TICKETS_FILE="tickets/prismform.json"   # uncomment once this project has a tickets file
